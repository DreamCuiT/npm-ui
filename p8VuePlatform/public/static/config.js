const CONFIG = {
  devBaseUrl: 'http://localhost:9092', // 第一次执行参考以上备注
  prodBaseUrl: 'http://localhost:9090'
}

// eslint-disable-next-line no-unused-vars
const SYSTEM_NAME = '八院厂所版项目管理系统'

// 在生产环境检测错误给出提示，默认为fale 意为生产环境不提示
// eslint-disable-next-line no-unused-vars
const CHECK_PRODUCTION_ERROR_TIP = false
